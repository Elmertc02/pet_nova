{
  "name": "etiquetas2",
  "version": "0.1.0",
  "private": true,
  "description": "Monorepo -- backend/ (Express+Postgres) y frontend/ (React+Vite) son proyectos independientes, cada uno con su propio package.json/node_modules. Este package.json de la raiz solo orquesta correrlos juntos en desarrollo.",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "backend": "npm run start --prefix backend",
    "frontend": "npm run dev --prefix frontend",
    "dev:all": "concurrently -n backend,frontend -c blue,green \"npm:backend\" \"npm:frontend\"",
    "build": "npm run build --prefix frontend",
    "test": "npm run test --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^10.0.4"
  }
}

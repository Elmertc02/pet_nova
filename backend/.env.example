PGHOST=localhost
PGPORT=5432
PGDATABASE=etiquetas2
PGUSER=etiquetas2_app
PGPASSWORD=your_db_password_here

# Use this file as a template. DO NOT commit secrets. Rename to `.env` and fill values.
PGHOST=localhost
PGPORT=5432
PGDATABASE=etiquetas2
PGUSER=etiquetas2_app
PGPASSWORD=
